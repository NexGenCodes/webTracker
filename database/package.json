{
  "name": "webtracker-db-mgr",
  "version": "1.0.0",
  "description": "Database management for WebTracker",
  "main": "migrate.mjs",
  "type": "module",
  "scripts": {
    "migrate": "node --env-file=.env migrate.mjs",
    "reset": "node --env-file=.env reset.mjs && npm run migrate"
  },
  "dependencies": {
    "postgres": "^3.4.8"
  }
}
